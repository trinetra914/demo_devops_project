apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "demo-devops.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "demo-devops.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "demo-devops.name" . }}
    spec:
      containers:
        - name: {{ include "demo-devops.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.containerPort }}
