apiVersion: apps/v1
kind: Deployment
metadata:
  # CHANGE THIS: Use 'fullname' for consistency with service.yaml
  name: {{ include "demo-devops.fullname" . }} 
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      # CHANGE THIS: Use 'fullname' here
      app: {{ include "demo-devops.fullname" . }} 
  template:
    metadata:
      labels:
        # CHANGE THIS: Use 'fullname' here
        app: {{ include "demo-devops.fullname" . }} 
    spec:
      containers:
        - name: {{ include "demo-devops.fullname" . }}
# ... (rest of the file)
