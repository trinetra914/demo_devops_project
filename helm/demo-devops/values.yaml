# --- FINAL-FINAL CORRECTED values.yaml ---

# 1. Disable Ingress (Correct)
ingress:
  enabled: false 

# ... (Image and Deployment config are fine) ...

# 3. Service Configuration
service:
  type: ClusterIP       # <--- CHANGE BACK TO CLUSTERIP
  port: 80              # Service ClusterIP Port
  targetPort: 80        # Container Port
  # REMOVE THE nodePort LINE ENTIRELY
